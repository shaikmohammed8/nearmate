import 'package:flutter/material.dart';

class ChatScren extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("chatscren"),
      ),
    );
  }
}
